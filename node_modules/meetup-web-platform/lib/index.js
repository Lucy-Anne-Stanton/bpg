'use strict';

var _serverRender = require('./renderers/server-render');

var _serverRender2 = _interopRequireDefault(_serverRender);

var _server = require('./server');

var _server2 = _interopRequireDefault(_server);

var _api = require('meetup-web-mocks/lib/api');

var apiMocks = _interopRequireWildcard(_api);

var _app = require('meetup-web-mocks/lib/app');

var appMocks = _interopRequireWildcard(_app);

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

// IMPORTANT: only export server-side modules here - browser
// should only import modules individually
module.exports = {
	makeServerRenderer: _serverRender2.default,
	startServer: _server2.default,
	mocks: {
		app: appMocks,
		api: apiMocks
	}
};